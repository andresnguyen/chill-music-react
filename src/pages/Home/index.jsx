import React from 'react'
import './Home.scss'

Home.propTypes = {}

function Home() {
    return <div className="main">{/* <Slider axis="x" x={value} onChange={({ x }) => setValue(x)} /> */}</div>
}

export default Home
